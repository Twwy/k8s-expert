### 任务内容

**任务标题**: 准备工作  
**任务ID**: task1  

**任务描述**:
1. 确认已经下载了 `sreworks-0.1.1.tgz` 包，并且安装环境已准备好。
2. 使用文本编辑器打开从 `sreworks-0.1.1.tgz` 包中解压出的 `sreworks/charts/appmanager/templates/server-configmap.yaml` 文件。
3. 在 `server-configmap.yaml` 文件中找到第36行附近，确认 `appmanager.server.jwtSecretKey` 是否被正确设置或遗漏。
4. 如果 `jwtSecretKey` 未设置，请为其提供一个合适的值。注意保持该密钥的安全性。

**相关文件**:
- `sreworks-0.1.1.tgz`
- `sreworks/charts/appmanager/templates/server-configmap.yaml`

请根据以上步骤执行任务，并确保所有配置正确无误。