### 任务详情

**任务标题**: 查看并修改配置  
**任务描述**: 请打开位于 `charts/appmanager/templates/` 路径下的 `server-configmap.yaml` 文件，检查第36行附近关于 `appmanager.server.jwtSecretKey` 的配置。根据实际需求，如果该参数不存在，请添加；若已存在但值不符合要求，请进行修正。

**相关文件路径**: `charts/appmanager/templates/server-configmap.yaml`

确保在执行此任务时遵循以下步骤：
1. 打开指定位置的 `server-configmap.yaml` 文件。
2. 定位到文件中的第36行或其附近区域。
3. 查找 `appmanager.server.jwtSecretKey` 参数。
4. 如果找不到该参数，则需将其添加进去，并设置合适的值。
5. 如果找到了该参数但其值不正确，请更新为正确的值。
6. 保存对文件所做的任何更改。

以上即为本次任务的具体内容，请严格按照指示操作。